{
  "name": "carbon-credit-blockchain",
  "version": "1.0.0",
  "description": "Blockchain contracts for carbon credit trading with ZK-SNARKs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "setup": "hardhat run scripts/setup.js --network localhost",
    "compile-circuits": "hardhat circom"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "circomlib": "^2.0.5",
    "dotenv": "^16.5.0",
    "ethers": "^5.7.2",
    "hardhat": "^2.19.0",
    "hardhat-circom": "^3.3.2",
    "snarkjs": "^0.7.5"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-verify": "^2.0.14",
    "@openzeppelin/contracts": "^4.9.6"
  }
}
